apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-logger
spec:
  selector:
    matchLabels:
      app: crud-logger
  template:
    metadata:
      labels:
        app: crud-logger
    spec:
      containers:
      - name: crud-logger
        image: imagem
        ports:
        - containerPort: 8080
